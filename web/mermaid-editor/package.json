{
  "name": "markup-view-mini-mermaid-editor",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run --environment jsdom",
    "build": "node build.mjs",
    "check:bundle": "node build.mjs --check",
    "check": "vitest run --environment jsdom && node build.mjs --check"
  },
  "dependencies": {
    "@markup-view-mini/mermaid-safe": "file:../shared/mermaid-safe",
    "codemirror": "6.0.2",
    "mermaid": "11.16.1"
  },
  "devDependencies": {
    "esbuild": "0.28.1",
    "jsdom": "29.1.1",
    "vitest": "4.1.10"
  }
}
